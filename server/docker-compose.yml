services:
  app:
    build: .
    ports:
      - "4888:4888"
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - app-data:/app/data

volumes:
  app-data:
